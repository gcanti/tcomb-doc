/*
var Alias = Str;

var OptStr = maybe(Str);

var Tags = list(Str);

var U = union([Str, maybe(Num)]);

var Person = struct({
  name: Str,
  surname: Str
});

var Positive = subtype(Num, function (n) {
  return n > 0;
});
*/

var MyEnum = enums({
  a: 1,
  b: 2
});